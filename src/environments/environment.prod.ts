export const environment = {
    production: true,
    apiUrl: 'https://api.videosynthesis.es/api'
};

//this is going to be only for production
// How Angular uses them:
// When you run ng serve on your laptop, Angular automatically loads environment.ts.
// When you run ng build to deploy your site live, Angular automatically swaps in environment.prod.ts.